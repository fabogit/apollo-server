# apollo-server

A basic GraphQl standalone server example that use Apollo Sandbox to execute GraphQL queries

### Install & run

`pnpm i`

`pnpm run start`

http://localhost:4000 will open the Apollo Sandbox
